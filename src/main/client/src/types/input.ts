export enum fieldEnum {
    Text = "Text",
    Number = "Number",
    Check = "Check",
    Select = "Select",
    Date = "Date"
};

export type inputFieldValueType = string | string[] | number | boolean | Date ;